أنت تعمل الآن داخل مشروع SaaS اسمه: **HAIROTICMEN Writer** على Replit.

هدفك: إنشاء **حزمة وثائق كاملة بالعربية** (Markdown) توضّح كل شيء عن المشروع:
- كيف يستخدمه الفريق (Product / Business / Content).
- كيف يطوّره المطوّرون (Backend + Frontend + DB + AI).
- كيف يُنشَر ويُصان (DevOps / Deployment / Maintenance).

🔹 أولاً – طريقة العمل العامة
1. اقرأ الكود كاملًا قدر الإمكان:
   - مجلدات: src / app / server / backend / prisma / db / lib / components / pages / routes (أو ما يعادلها حسب المشروع).
   - ملفات التهيئة: package.json، tsconfig.json، next.config / vite.config / replit.nix، وأي ملف env.example أو README موجود.
2. لا تعتمد على الإنترنت؛ اعتمد فقط على ما هو موجود في المستودع.
3. إذا وجدت وثائق سابقة (README, docs, .md files) فقم:
   - بدمجها.
   - وتحديثها.
   - وتوحيد أسلوب الكتابة بالعربية.
4. اكتب كل شيء في **ملف واحد رئيسي** باسم:
   `docs/HAIROTICMEN-Writer-Full-Guide-AR.md`
   (وإذا لم يوجد مجلد docs اعتبره مجلدًا افتراضيًا، فقط اكتب المحتوى كـ Markdown في الإخراج).

💡 لغة الوثائق: العربية الفصحى الواضحة، مع إبقاء أسماء الملفات والمتغيرات والـ APIs بالإنجليزية كما هي.

------------------------------------------------
الجزء 1 — نظرة عامة على النظام (System Overview)
------------------------------------------------
أنشئ قسمًا بعنوان واضح مثل:
`# نظرة عامة على نظام HAIROTICMEN Writer`

اشرح فيه بالتفصيل:
1. فكرة المشروع ووظيفته الأساسية:
   - نظام كتابة وإدارة مستندات متعدد اللغات (AR/EN/DE).
   - واجهة شبيهة بـ Fluent / Productivity Workspace.
2. أقسام الواجهة الرئيسية (حسب الكود وملفات الـ routes):
   - Dashboard
   - AI Writer (أو IWRITE إذا كان الاسم محدثًا في الكود)
   - Songwriter
   - Documents
   - File Uploads
   - Templates
   - Style Profiles
   - Search
   - Topics
   - Archive
   - Settings
3. التقنية العامة:
   - إطار العمل (Next.js / React / Express / إلخ).
   - لغة البرمجة (TypeScript أو JavaScript).
   - قاعدة البيانات (PostgreSQL أو غيرها) + ORM المستخدم (Prisma مثلاً).
   - بنية الـ API (REST / RPC / tRPC / إلخ).
4. مخطط معماري نصّي:
   - Frontend ↔ API ↔ Database
   - أين يتم استدعاء نموذج الذكاء الاصطناعي (OpenAI أو غيره)، وكيف يمرّر الـ prompt والـ result.

استخدم نقاط وتعداد وجداول بسيطة قدر الإمكان.

------------------------------------------------
الجزء 2 — دليل الاستخدام لكل قسم (Product / UX Guide)
------------------------------------------------
لكل قسم رئيسي في الـ Sidebar أنشئ فقرة فرعية كالتالي:

## 2.X – قسم AI Writer / IWRITE
- الهدف من القسم: توليد/تلخيص/إعادة كتابة المستندات بشكل احترافي.
- عناصر الواجهة:
  - Prompt Box (حقل الوصف).
  - Task Presets (New Document from Sources / Summarize / Rewrite / Translate / Generate Variants… حسب ما تجده في الكود).
  - Document Type
  - Target Length
  - Formality Level
  - Language
  - زر Generate Draft
- سلوك العمل (Workflow):
  1. المستخدم يكتب الطلب في prompt.
  2. يختار Preset ونوع المستند و اللغة ودرجة الرسمية.
  3. يتم استدعاء خدمة الـ AI في الـ backend.
  4. يتم حفظ النتيجة كـ Document مرتبط بالمستخدم + Template + Style Profile (إذا كانت متصلة في الكود).
- ملاحظات عن الجودة:
  - كيف يتم تمرير style profile أو template داخل الـ prompt (استنتج من خدمة الـ AI في الكود).

ثم كرّر نفس النمط لباقي الأقسام:

### Songwriter
- هدفه: كتابة كلمات أغاني/نصوص شعرية بناءً على الفكرة + النمط + اللغة.
- الحقول المتاحة (genre, mood, language, structure… إذا كانت موجودة).
- كيف يتم حفظ الأغنية كنص/ملف.

### Templates
- ما الذي يديره هذا القسم (الرأس، التذييل، الألوان، الشعار، معاينة الورقة).
- الحقول: Template Name، Header، Footer، Logo URL / Upload، Brand Colors، خيارات حجم الخط، مكان الشعار (In Header / Top Left / Top Center / …).
- أين يتم تخزين البيانات (جدول templates في DB أو ملف JSON) وكيف تُستخدم عند توليد مستند جديد.

### Style Profiles
- الحقول: Profile Name، Tone، Voice، Writing Guidelines.
- كيف يتم ربطها بالـ AI Writer/Songwriter داخل الكود (fn أو service معينة).

### Documents
- كيف يتم عرض قائمة المستندات، الفرز، البحث، الإصدارات إن وجدت.

### File Uploads
- أنواع الملفات المسموح بها (PDF, DOCX, CSV, XLSX, Images… إلخ حسب الكود).
- حجم الملف الأقصى.
- أين تُخزن البيانات الوصفية في DB (file records / uploads table).
- كيف يتم ربط الملفات بموضوعات (Topics) أو مستندات.

### Search & Topics & Archive
- شرح مختصر لكيفية البحث (keyword-based، وليس embeddings إذا كانت محذوفة).
- كيف يعمل نظام Topics (جداول topic + topic_keywords + document_segments أو ما يشبهها).
- مفهوم الأرشيف: ما الذي يحدث عندما يؤرشف المستخدم عنصرًا.

### Settings
- إعدادات اللغات، واجهة المستخدم، API keys (إن وُجدت في الكود بطريقة آمنة).
- أي toggles أو flags متعلقة بالـ AI أو PDF Export.

لكل قسم:
- وضّح: المدخلات، المخرجات، أي حدود (Limits)، وأي سلوك ذكي/أتمتة موجودة.

------------------------------------------------
الجزء 3 — التوثيق التقني للمطورين (Developer Technical Docs)
------------------------------------------------
أنشئ فصل بعنوان:
`# التوثيق التقني للمطورين`

واشرح فيه:

1. **بنية المجلدات (Folder Structure)**
   - جدول يوضّح أهم المجلدات:
     - `src/` أو `app/`: ما الذي يحتويه.
     - `server/` أو `api/`: الخدمات الخلفية.
     - `db/` أو `prisma/`: تعريفات قاعدة البيانات.
     - `components/`: مكونات الواجهة المشتركة.
     - `lib/` أو `utils/`: الدوال والخدمات المساعدة.
   - لكل مجلد، سطر يشرح دوره.

2. **نموذج البيانات (Database Schema)**
   - استخراج الجداول من ملفات Prisma أو migrations أو schema.sql.
   - عمل جدول/قائمة بأهم الجداول، مثل:
     - users
     - documents
     - templates
     - style_profiles
     - file_uploads
     - topics / topic_segments …
   - لكل جدول:
     - الحقول (الاسم، النوع، إن كانت إجباري/اختياري).
     - العلاقات (1–N، N–M) مع الجداول الأخرى.

3. **الـ APIs / Endpoints**
   - استخرج الـ routes/handlers في الـ backend:
     - مثل: `POST /api/ai/generate`, `GET /api/documents`, `POST /api/templates`, إلخ.
   - لكل endpoint:
     - الهدف.
     - طريقة الطلب (GET/POST/PUT/DELETE).
     - Body المتوقع (JSON).
     - Response الرئيسي.
   - إذا كان هناك طبقة خدمة (service layer) فاشرح بإيجاز كيف تُستدعى من الـ API.

4. **طبقة الذكاء الاصطناعي (AI Layer)**
   - ملفات الخدمات المسؤولة عن الاتصال بـ LLM (OpenAI أو غيره).
   - كيف يُبنى الـ prompt من:
     - محتوى المستخدم.
     - Template المختار.
     - Style Profile.
     - لغة الهدف (AR/EN/DE).
   - أي حمايات أو حدود (max tokens, temperature, إلخ) إذا ظهرت في الكود.

5. **التدويل واللغات (i18n & RTL)**
   - طريقة التعامل مع اللغات الثلاث (AR/EN/DE):
     - هل يوجد i18n config أو ملفات ترجمة.
     - كيف يتم دعم RTL في الواجهة (className / direction).

6. **التعامل مع الملفات (File Handling)**
   - أين يتم حفظ الملفات فعليًا (Local disk / Cloud storage).
   - كيف يتم استخراج النص من PDF/DOCX/CSV/XLSX (أي مكتبات مستخدمة).
   - حدود الحجم وعدد الصفحات إن وجدت في الكود.

7. **الأمان (Security)**
   - آلية التحقق من الهوية إن وجدت (Auth / Sessions).
   - سياسات CORS، rate limiting أو حماية endpoints المهمة.
   - التعامل مع مفاتيح الـ API (بيئة env فقط، عدم الحفظ في DB).

------------------------------------------------
الجزء 4 — تشغيل المشروع ونشره (Run, Build, Deploy)
------------------------------------------------
أضف فصل بعنوان:
`# تشغيل المشروع، البيئة، والنشر`

اشرح فيه استنادًا إلى ملفات التهيئة و package.json:

1. **أوامر التشغيل:**
   - `npm run dev` للتطوير.
   - `npm run build` للبناء.
   - `npm run start` للإنتاج (إن وُجد).
   - أي سكربتات أخرى (lint, test, seed).

2. **البيئة (Environment Variables):**
   - استخرج أسماء المتغيرات من الكود (مثال: OPENAI_API_KEY, DATABASE_URL, etc).
   - وضّح وظيفة كل متغير باختصار، بدون كشف أي قيم حقيقية.
   - أنشئ مثالًا لملف `.env.example` في الوثيقة.

3. **متطلبات التشغيل:**
   - إصدار Node.js.
   - قاعدة البيانات (PostgreSQL إلخ).
   - أي خدمات خارجية (OpenAI, Replit DB, Supabase…).

4. **سيناريو تشغيل سريع (Quickstart):**
   - خطوات مرتّبة من الصفر:
     1. استنساخ المشروع.
     2. تثبيت الحزم `npm install`.
     3. إعداد قاعدة البيانات (migrate / seed).
     4. إعداد ملف `.env`.
     5. تشغيل `npm run dev`.
     6. فتح رابط localhost.

5. **ملاحظات النشر (Deployment Notes):**
   - ما الذي يحتاجه المشروع ليعمل على خادم إنتاج (Environment, DB, build step).
   - أي إعدادات خاصة لـ Replit إن كانت مذكورة في ملفات المشروع.

------------------------------------------------
الجزء 5 — دليل للمطورين الجدد (Contribution & Coding Standards)
------------------------------------------------
أنشئ قسم بعنوان:
`# دليل المطورين الجدد ومعايير الكود`

وضّح فيه:

1. **أسلوب الكود (Coding Style):**
   - استخدام TypeScript/ESLint/Prettier إن وجدت.
   - نمط تسمية الملفات والمكونات (PascalCase للمكونات، camelCase للدوال…).

2. **إضافة ميزة جديدة (Feature Workflow):**
   - أين يضيف المطوّر:
     - المكونات React.
     - الـ API endpoint.
     - التغييرات في DB (Prisma schema أو migrations).
   - كيف يختبر محليًا.

3. **التعامل مع الـ AI في الميزات الجديدة:**
   - كيف يُبنى prompt جديد.
   - كيف يحقن Template/Style Profile.
   - أفضل ممارسة لتفادي الردود غير المرغوبة (تحديد لغة، نبرة، طول النص).

4. **إدارة الإصدارات والـ Git (إذا كان هناك تلميح في المشروع):**
   - فرع رئيسي main/master.
   - اقتراح لنهج فروع feature.

------------------------------------------------
الجزء 6 — اقتراحات وثائق إضافية لتحسين الإنتاج
------------------------------------------------
في نهاية الملف، أضف قسمًا بعنوان:
`# اقتراحات لتحسين التوثيق والمنتج`

اذكر فيه – بناءً على الكود وما رأيته – ما ينقص من الوثائق، مثل:

1. وثيقة مستقلة لـ:
   - **AI Writer Prompt Library**: مجموعة أمثلة جاهزة للـ prompts حسب نوع المستند (عقود، عروض أسعار، كتالوج، سياسة خصوصية…).
   - **Songwriter Guide**: كيف يكتب المستخدم أفكار الأغنية للحصول على أفضل نتيجة.

2. توثيق خاص بـ:
   - نظام Topics والبحث المستند إلى المقاطع.
   - تكامل النظام مع ملفات HAIROTICMEN (كتالوج المنتجات، OS, Loyalty, Stand Program…) إذا كان الكود يشير لها.

3. خطة تطوير مستقبلية (Roadmap مختصرة) استنادًا إلى التعليقات في الكود أو TODOs.

أضف في نهاية الملف **Checklist** بسيطة:
- [ ] تم توثيق كل الأقسام في الواجهة.
- [ ] تم شرح الـ DB Schema.
- [ ] تم شرح الـ API.
- [ ] تم توثيق تشغيل المشروع.
- [ ] تم إضافة اقتراحات لتحسين التوثيق.

✅ هدف هذا الطلب:
إخراج ملف Markdown واحد شامل بالعربية يمكن استخدامه:
- كمرجع رسمي للمنتج.
- وكدليل كامل لأي مطوّر جديد ينضم للمشروع.
